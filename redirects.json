{
  "schema_version": "0.13.0",
  "note": "Text ids that no longer name what they used to. APPEND ONLY. A word's global address is `<text-id>.<word-id>` (SCHEMA.md), and the mint made the second half permanent. This is the first half: a renamed text would otherwise break every apparatus reference, every review seal, every SRS card and every URL that named it. `to` may be null for a text that was withdrawn rather than renamed.",
  "moved": []
}
